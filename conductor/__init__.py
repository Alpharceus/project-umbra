"""
Project Umbra — Conductor Package

The Conductor is the authoritative server that manages device discovery,
room layout, theme state, global timing, brightness, sleep state, and
dashboard requests. It serves the player and dashboard apps over HTTP
and maintains WebSocket connections for real-time state synchronization.
"""
